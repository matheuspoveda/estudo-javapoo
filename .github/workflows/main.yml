name: DemoNewCD
on:
  repository_dispatch:
  
  
jobs:
  build:
    runs-on: ubuntu-lastest
    steps:
      - name: Run a one-line script
        run: echo Hello, world!
        

  deploy-dev:
    runs-on: ubuntu-lastest
    environment:
      name: dev
      url: 'matheuspoveda.com'
    needs: build
    steps:
      - name: Run a one-line script
        run: echo Deploy Dev
